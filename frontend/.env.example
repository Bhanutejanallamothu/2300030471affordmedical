VITE_API_BASE_URL=http://localhost:4000
VITE_STUDENT_ID=1042

# Optional: set these only if you want the browser to post directly to the
# evaluation service instead of using the backend proxy.
VITE_EVALUATION_LOG_URL=
VITE_EVALUATION_LOG_TOKEN=
